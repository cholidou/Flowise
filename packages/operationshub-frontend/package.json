{
  "name": "crpflow-control-center",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone CRPflow Control Center frontend (no Flowise runtime required)",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  }
}
